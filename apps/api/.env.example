# Flow API — copy to .env for local development
PORT=8787
DATABASE_URL=postgresql://flow:flow@localhost:5432/flow
REDIS_URL=redis://localhost:6379
# Auth is stubbed until Phase 10; anonymous WS connections allowed in dev.
REQUIRE_AUTH=false
